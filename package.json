
{
  "name": "connect-telecom-monorepo",
  "private": true,
  "packageManager": "npm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "install:all": "npm install"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
